meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{baseURL}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "ctrix@ctrix.pro",
    "password": "password123"
  }
}

settings {
  encodeUrl: true
}
